#include <iostream>
#include <string.h>

int main()
{


   /* char alumno1[16]="Antonio\n";
    printf("%s", &alumno1);
    fgets(Antonio, sizeof(Antonio), stdin);

    char alumno2[16]="Diego\n";
    printf("%s", &alumno2);
    fgets(Diego, sizeof(Diego), stdin);*/

    char jugadores[3][20];

    strcpy(jugadores[0], "Ana");
    strcpy(jugadores[1], "Luis");
    strcpy(jugadores[2], "Carlos");

    char jugador[5][16];
    int i;
    int longitud;
    longitud=strlen(jugador[0]);

    for(i=0; i<4; i++)
    {
        printf("anota los jugadores: \n");
        fgets(jugador, sizeof(jugador), stdin);
        printf("la longitud %s, es %d",jugador[0],longitud);
    }



    return 0;
}
